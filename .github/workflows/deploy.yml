name: Deploy to Hostinger Using GitHub Actions

on:
  push:
    branches:
      - main # The branch name your are commiting the new changes
jobs:
  build_deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v4

      - name: Install sshpass
        run: |
          sudo apt-get update
          sudo apt-get install -y sshpass

      - run: |
          sshpass -p ${{ secrets.PASSWORD }} ssh -o StrictHostKeyChecking=no -p ${{ secrets.PORT }} ${{ secrets.USERNAME }}@${{ secrets.HOST }} 'cd domains && ls -l && chmod 777 wassimkaouia.online && ls -l && cd wassimkaouia.online/public_html && ls -l && rm -rf * && rm -rf .git && rm -rf .github && git init && git remote add origin https://github.com/wassim-kaouia/my-devops-portfolio.git && git pull origin master && composer install && composer require laravel/ui && php artisan migrate'